Q:1 Tell me Something about yourself ?
Ans: I'm a BE developer with 3+ years of experience specialsing in Nodejs, ExpressJs, PostgreSQL, Typescript/Javascript. At Ahex Technologies I have built Scalable REST & GraphQL APIs, integrated AI/LLM services & Optimized DBs reducing API latency by 30-35% & improving DB efficiency by 20-25%. My most relevant projects is MayBeAtHome where I have implemented OpenAI Semantic search that improved property match accuracy by 35-40%. I'm exiting about this role bcoz ...

Q:2 Tell me about a time when you debugged a complex issue under pressure?
Ans: 
- Last year on july it was a Monday morning I still remember that morning when I was working on TalentPrise when thousands of users where active, our API response time suddenly become 5x slower.
- So my responsibility was to quciky find the r       oot cause and fix it without affecting the users much.
- So firstly what I did is I looked into the monitoring metrices in AWS Cloudwatch & I got to know that Chat microservice is taking most of the time. Then I looked deeper & discovered that database query was running a full table scan bcoz an index was accidently removed during a migration.
- Then I added that index back to the Database & I also temporary increased the redis TTL time so that fewer request will hit the DB while the fix was deploying.
- The result is within 20 minutes the response went back to normal.
- Later on  we have added automatic query plan checks in the CI pipeline to make sure missing indexes are caught before the deployment.

Q:3 Describe a situation where you had an ambiguous requirement? How did you handled it?
Ans:
Recently it happens to me when we were working on May be at Home project our Project Manager asked us to implement Smart Search, but there were no clear technical specifications about what exactly need to change.

- So our goal was to understand the real problem and come with the solution that actually helps the users.
- So instead of guessing what I did is I arranged a discussion with project owner and some of my collegues and we came to know about the problem the problem was keyword search couldn't understand the user intent.
- So what we decided is to build a Semantic Search prototype using Vector Embeddings from OpenAI.
- We further discuss this with the Stakeholder they liked the idea and approved it.
- We build the final version in 2-3 weeks of time.
- From this  what I learned is instead of making the guess & waiting for the perfect specifications for ambigous requirement its better to talk with the stakeholder & build a quick prototype.

Q:4 How do you handle working on multiple customer engagements simultaneously?
Ans: 
- Time management.
- Documentation.
- Communication.
- Understanding the domain. 

Q:5 Explain Memory Management & Potential Memory Leaks in Nodejs?
Ans: Nodejs manages memory automatically using the garbage collector in the v8 engine, which uses a mark & sweep algorithm approach to remove the unused objects. However, memory leaks can still happen if refrences are unintentionally kept in the application.

- Common causes of Memory Leaks are:
- Unclosed browser instances in Puppeteer.
- Unresolved Promise holding closures.
- Accumulating Event Listeners.
- Large Unbounded Caching.
- We should also avoid Global Variables & Timers and Intervals.

For Debugging Memory Leaks I use:
- I run nodejs with --inspect flag to take the Heap snapshots in Chrome DevTools.
- We also use clinic.js, especially tools like  Doctor & Heap Profiler to analyze Memory Usage & detect performance bottlenecks in Nodejs applications.

setImmediate(() => {
    process.memoryUsage()
})

Q:6 Exaplin How JS is Single Threaded? What is Event Loop & How does it work?
Ans: Javascript is single threaded which means it can execute one task at a time using the callstack. To handle the async tasks like Timers, API calls or file operations JS make uses of event loop.

- Event loop is a mechanism in JS which is use to handle the async operations, instead of waiting for the longer taks like Timers, API calls or file operation JS make use of an event loop which handle this tasks on the background while contiue executing the other parts of the code, this make JS efficient & non-blocking.

- CallStack is where the Sync code runs. Callstack is the collection of functions which are under execution, if no functiona are are there Global Anonymous function are present once it finishes the sync code execution it picks the tasks from Microtask Queue (Promises) & then from Macrotask Quere(Set time functions). Microtask Queue have > priority then Macrotask Queue.

ex: 
setTimeout(() => console.log("timeout"), 0);
Promise.resolve().then(() => console.log("promise"));
o/p: promise, timeout.

Q:7 Difference Between Promise.all, Promise.allSettled, Promise.race, Promise.any?
Ans: 
- Promise.all(): Waits for all promise to get succeed, If one fails, all promise fails immediately.
Usecase: when u need all results together.

- Promise.allSettled(): Waits for all promises to get finished, Doesn't fail if any of the promise get rejected, return status + value/reason.
Usecase: When u want results even if some fails.

- Promise.race(): Returns results of first promise that settled (resolve or rejected).
Usecase: Timeout pattern.

- Promise.any(): Return the first successful promise, Ignores rejected one, Fails only if all promises rejected.
Usecase: Fallback strategy.
Ex: Try multiple APIs return whichever works fine.

Q: Why should we hire you?
Ans: I have strong backend fundamentals with Node.js, including building scalable APIs, handling authentication, and optimizing performance. Along with that, I also have a good understanding of React, including hooks, state management, and component design. I focus on writing clean, maintainable, and reusable code, and I’m comfortable working across both frontend and backend. I’m also quick to adapt to new technologies and enjoy learning new tools as needed. 

Q: How do you reduce feature development by 35% ?
Ans: Earlier services had tightly coupled modules & repeated logic across controller & services. I refactored the BE into modular services following the clean architecture principles. Shared functionality like authentication guards, error handling, validation pipes, DB utility were extracted into reusable modules. 
Because of this new features can reuse this existing components of rewriting logic, which can reduce development time significantly & made feature delivery faster.

=====================================================================================================================================
Q: How did you reduce API latency by 20-30%?
Ans: We identified slow endpoints using proper loggging & query analysis. So the main bottleneck was repeated db queries & large payloads responses.
- Using proper indexing.
- Removing unnessary joins.
- Redis caching for frequently accessed data.
- Using pagination or Curson based pagination.
- async code.

=====================================================================================================================================
Q: How did redis reduce response time by 30%?
Ans: We observed that some endpoints like user profile, following/followers & frequently accessed lists were being queried from the database on every request. So I implemented redis caching for those endpoints using a cache aside strategy. When a request comes we check the redis first if the data is present we return it directly else we fetch data from database and cache it in redis. This reduce the database load and improve the response time significantly.

======================================================================================================================================
Q: How did you imrpove database Read/Write efficiency by 30%?
Ans: initially some queries were doing full table scans & inefficient joins.
- I analyzed the slow queries using database logs & EXPLAIN plans. Then I added appropriate indexes on frequently filtered columns & optimized query structures.
- I also reduced unnecessary queries by doing batch operations & using proper rlsnships, which improved Read & Write efficiency.

======================================================================================================================================
Q: How did you deliver features ahead of schedule?
Ans: From the beginning I build reusable modules & well structured services. Instead of implementing everything from scratch for each feature. I reused existing Authentication modules, error handling, db utility, validation pipes etc. This all reduced the implemntation time & allowed us to ship the several BE features faster than the planned sprints.

======================================================================================================================================
Q: What kind of microservices architecture you used?
Ans: We have used modular microservices architecture where each service handled a specific domain like authentication, notification, chats etc. 
Services communicated through grpc & common concerns like logging, authentication, validation were standardized across services. This made the system easier to scale and maintain.

======================================================================================================================================
Q: How did you ensured system scalability?
Ans: Scalability was achieved through stateless BE system, using DB indexing, caching through redis & seprating services based on domains. This allowed us to scale each services independently depending on the traffic requirements.

======================================================================================================================================
Q: How did CI/CD reduce deployment time?
Ans: Earlier deployments were manual & error prone. So We have implemented CI/CD using github action, CI/CD is a process where you application code is automatically build, tested & deployed in the server. Whenever we will push the code in the development branch the pipeline automatically build the application & deployed it to the server.
This reduced deployment time & minimized the manual errors.
=====================================================================================================================================
Q: How did you implemented RBAC?
Ans: We have implemented RBAC using Tables we have defined two tables Roles and Permissions. Each user was assigned a role & each role has a specific permission mapped into the API actions.
Middleware & Guards were used to check the permission before allowing access to the protected routes.

======================================================================================================================================
Q: How did you supoort 50k daily active users?
Ans: To support large traffic users we focused on optimizing APIs, Redis caching, DB indexing Pagination and async codes and also ensuring services were stateless so that they could scale easily.
We also implemented efficient logging & monitoring to identify performance bottlenecks quickly.

======================================================================================================================================

Q: What happens inside nodejs when an API request comes?
Ans: When a request hits the server, Nodejs receives it through the HTTP server modules. The request is placed in the event loop queue. If the request requires I/O operations like database queries or external API calls, those operations are delegated to the Node.js worker threads through libuv. While waiting for the response, the event loop continues processing other requests. Once the I/O operation completes, the callback or promise is placed back in the event loop and the response is returned to the client.

======================================================================================================================================
Q: Why is nodejs good for high concurrency applications?
Ans: Nodejs is good for high concurrency applications bcoz of it Non-Blocking I/O event driven architecture. Instead of creating thread for each & every request like traditional servers. Nodejs uses a single threaded event loop to handle all the request async.

======================================================================================================================================
Q: Difference between Express & NestJS?
Ans:
- ExpressJs is a Leightweight framework, comes with minimal str, manual dependency mgmt, No default architecture good for small applications.
- NestJS is Full BE framework built on the top of expressJS, comes with Optimized str, Built in dependency injection, Modular architecture and good for scalable applications.

======================================================================================================================================
Q: What is Dependency Injection in NestJS?
Ans: Dependency Injection is a design patterns where dependencies are provided to a class instead of being created inside it.

======================================================================================================================================
Q: How would you design a scalable authentication system?
Ans: A scalable authentication system typically includes JWT-based authentication, secure password hashing, refresh tokens, and centralized user management. Passwords are stored using hashing algorithms like bcrypt or argon2. Access tokens are used for API authentication, and refresh tokens allow issuing new access tokens without requiring the user to log in again. For large systems, session storage or token revocation lists may be managed using Redis.

======================================================================================================================================

Common Redis use cases:
API caching
session storage
rate limiting
pub/sub messaging

======================================================================================================================================
Q: What is the differenece between SQL & No SQL?
Ans: 
SQL: Structured Schema, Strong Consistency, Relational Tables, Postgres, MYSQL.
No-SQL: Flexible Schema, Better for large scale distributed systems, Document or key-value storage, MongoDB, Redis.

======================================================================================================================================
Q: How do you handle errors in production APIs?
Ans: Using Centralized error handling mechanism sensitive data should not get exposed in error responses, Error Handling Middlewares, with HTTP Status Codes, Winsten loggers etc.

======================================================================================================================================
Q: How do you handle Traffic Spikes in production APIs?
Ans: Handling large traffic spikes involves multiple strategies such as horizontal scaling, rate limiting, DB query optimization, caching frequently used data using Redis. Also Load Balancer can distribute traffic across multiple servers instances to maintain system scalability.
======================================================================================================================================
